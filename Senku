#include <iostream>
#include"senku.h"
using namespace std;
int main() {
  int x1,x2,y1,y2;
    tipomatriz tablero[nrow][ncol];
    crear_tablero(tablero,nrow,ncol);
    imprimir_tablero(tablero,nrow,ncol);
    mover_ficha(tablero,x1,x2, y1,y2);
    return 0;
}
